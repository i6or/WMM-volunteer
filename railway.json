{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "apt-get update && apt-get install -y python3 python3-pip && npm run build && pip3 install -r requirements.txt --target .pythonlibs",
    "watchPatterns": [
      "**"
    ]
  },
  "deploy": {
    "startCommand": "npm start",
    "healthcheckPath": "/api/health"
  }
}
